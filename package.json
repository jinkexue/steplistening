{
  "name": "steplistening",
  "version": "1.0.0",
  "description": "A full-stack dictation tool with Cloudflare Pages, D1, and R2",
  "scripts": {
    "dev": "wrangler pages dev . --d1 DB --r2 BUCKET",
    "deploy": "wrangler pages deploy .",
    "build": "echo 'No build step needed for static files'"
  },
  "dependencies": {
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.114.17"
  }
}
