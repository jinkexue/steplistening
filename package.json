{
  "name": "steplistening",
  "version": "1.0.0",
  "description": "A full-stack dictation tool with Cloudflare Workers, D1, and R2",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "esbuild src/index.ts --bundle --minify --sourcemap --outfile=dist/index.js --external:__STATIC_CONTENT_MANIFEST"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
